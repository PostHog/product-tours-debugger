name: Release

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Create release PR or publish
        id: changesets
        # v2 pushes release commits through the GitHub API by default
        # (push-with-git-cli: false), so they carry GitHub's signature, which the
        # org-wide signed-commits ruleset requires. Pinned to the v2 prerelease
        # because that mode is not in v1.9.0, the current stable release.
        uses: changesets/action@2d9f7af6402422be60d377f151c7618011bba45a # v2.0.0-next.4
        with:
          version: npm run changeset-version
          publish: npx changeset tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and upload extension zip
        if: steps.changesets.outputs.published == 'true'
        run: |
          VERSION=$(node -p "require('./package.json').version")
          ZIP_NAME="posthog-tour-debugger-v${VERSION}.zip"
          zip -r "$ZIP_NAME" \
            manifest.json \
            *.js \
            *.html \
            *.css \
            icons/ \
            images/
          gh release upload "v${VERSION}" "$ZIP_NAME" --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
